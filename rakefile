$LOAD_PATH.unshift(File.expand_path('../.', __FILE__))
require 'pry'
require 'install'

task :install do
  puts 'Runing simple install...'
  binding.pry
end

task :backup do
  Backup
end

task :clear_backup do

end
